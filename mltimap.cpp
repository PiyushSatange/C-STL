#include <bits/stdc++.h>
using namespace std;

int main(){

    //it is similer to the map only the difference is it stores the same elements in the sorted order
    //only mm[key] can not be use here because there can be multiple same keys
    multimap<int, int> mm;

    return 0;
}